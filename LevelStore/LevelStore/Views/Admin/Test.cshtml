@model IList<Image>
@{
    ViewData["Title"] = "Test";
    Layout = "_Layout";
}

<form asp-action="Test" asp-controller="Admin" method="post" >
    <div class="form-group">
        @for (int i = 0; i < 3; i++)
        {
            <label>Name:</label>
            <input name="[@i].Name" class="form-control"/>
        }
    </div>
    <button type="submit" class="btn btn-primary">Save</button>
</form>

